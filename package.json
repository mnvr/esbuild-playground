{
  "scripts": {
    "build": "esbuild app.jsx --bundle --outfile=dist/out.js",
    "build:js": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
